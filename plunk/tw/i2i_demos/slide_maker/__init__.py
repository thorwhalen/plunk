"""A setup for extrude play

How to use:

- Import or write your resources in the ``resources.py`` file.
- Still in ``resources.py``, assign to ``func`` the function you want to expose.
- If needed, modifry the ``configurations.py`` file.

- Run ``python server.py` to start the http server. And with that you can:
  - Go to http://localhost:3030/openapi to see the OpenAPI specs.
  - Go to http://localhost:3030/swagger to see the Swagger UI.

- Run ``streamlit app.py`` to get the GUI app.


"""
